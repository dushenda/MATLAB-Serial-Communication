function  dataDisp(obj,events,handles)
% bytes(obj,events,handles);
set(handles.txtTime,'String',datestr(now));
disp('dataDisp called');
% if handles.hasData
% data = bytes(obj,events,handles);
% disp(data);
% end
end

